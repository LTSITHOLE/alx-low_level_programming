#include <stdio.h>
#include <time.h>
#include <stdlib.h>
/**
 * main - a program that prints the alphabet in lowercase,
 * Return: 0 Success
*/
int main(void)
{
int n, m, o;
for (n = '0'; n < '9'; n++)
{
for (m = n + 1; m <= '9'; m++)
{
for (o = d + 1; o <= '9'; o++)
{
if ((m != n) != o)
{
putchar(n);
putchar(m);
putchar(o)
if (n == '7' && m == '8')
continue;
putchar(',');
putchar(' ');
}
}
}
}
putchar('\n');
return (0);
}
